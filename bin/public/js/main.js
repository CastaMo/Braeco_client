(function(window, document) {
  var Bottom, getById, query, querys;
  query = util.query;
  querys = util.querys;
  getById = util.getById;
  return Bottom = function() {
    var allDoms, state;
    state = 0;
    return allDoms = querys("#nav-field");
  };
})(window, document);
